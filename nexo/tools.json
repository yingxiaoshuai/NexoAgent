{
  "version": 2,
  "defaultEnabled": [
    "shell_command",
    "invoke_model",
    "recall_memory",
    "write_knowledge"
  ],
  "tools": [
    {
      "name": "shell_command",
      "label": "Shell Command",
      "group": "system",
      "description": "Run a shell command inside the configured workspace. Use this for filesystem inspection, CLI workflows, and scripted operations. Do not run broad recursive scans from drive or system roots unless the user explicitly asks and the path and depth are narrowed.",
      "parameters": {
        "type": "object",
        "properties": {
          "command": {
            "type": "string",
            "description": "Shell command to run"
          },
          "cwd": {
            "type": "string",
            "description": "Optional workspace-relative working directory"
          },
          "timeoutMs": {
            "type": "number",
            "description": "Optional timeout in milliseconds. Omit to use the configured default."
          }
        },
        "required": [
          "command"
        ]
      }
    },
    {
      "name": "invoke_model",
      "label": "Invoke Model",
      "group": "model",
      "description": "Call the default model or a specialist model profile. Route multimodal work through capability tags such as vision, image_generation, image_editing, speech_to_text, and text_to_speech.",
      "parameters": {
        "type": "object",
        "properties": {
          "profile": {
            "type": "string",
            "description": "Optional profile id or name, or default"
          },
          "capability": {
            "type": "string",
            "description": "Optional capability: orchestration, chat, vision, image_generation, image_editing, speech_to_text, text_to_speech, or embedding"
          },
          "prompt": {
            "type": "string",
            "description": "Text prompt or edit instruction. Required for normal chat, image analysis questions, image generation, and image editing."
          },
          "system": {
            "type": "string",
            "description": "Optional system instruction for text-model calls"
          },
          "temperature": {
            "type": "number",
            "description": "Optional temperature override for text-model calls"
          },
          "maxTokens": {
            "type": "number",
            "description": "Optional max output tokens for text or vision responses"
          },
          "images": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Optional image upload paths, URLs, or data URLs for capability=vision or capability=image_editing"
          },
          "audio": {
            "type": "string",
            "description": "Optional audio upload path, URL, or data URL for capability=speech_to_text"
          },
          "input": {
            "type": "string",
            "description": "Optional text input for capability=text_to_speech"
          },
          "detail": {
            "type": "string",
            "description": "Optional image detail for vision calls: auto, low, high, or original"
          },
          "n": {
            "type": "number",
            "description": "Optional number of generated images"
          },
          "size": {
            "type": "string",
            "description": "Optional image output size"
          },
          "quality": {
            "type": "string",
            "description": "Optional image output quality"
          },
          "background": {
            "type": "string",
            "description": "Optional image background mode"
          },
          "outputFormat": {
            "type": "string",
            "description": "Optional output format such as png, jpeg, or webp"
          },
          "inputFidelity": {
            "type": "string",
            "description": "Optional input fidelity for image editing: low or high"
          },
          "voice": {
            "type": "string",
            "description": "Optional voice for text-to-speech"
          },
          "instructions": {
            "type": "string",
            "description": "Optional speaking style instructions for text-to-speech"
          }
        }
      }
    },
    {
      "name": "recall_memory",
      "label": "Recall Memory",
      "group": "memory",
      "description": "Search persistent daily, dream, or script memory stored in SQLite with embedding-backed recall when available.",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "What to recall"
          },
          "kinds": {
            "type": "array",
            "items": {
              "type": "string",
              "enum": [
                "daily",
                "dream",
                "script"
              ]
            },
            "description": "Optional persistent memory kinds to search"
          },
          "dayKey": {
            "type": "string",
            "description": "Optional YYYYMMDD day filter"
          },
          "k": {
            "type": "number",
            "description": "Optional result limit"
          }
        },
        "required": [
          "query"
        ]
      }
    },
    {
      "name": "write_knowledge",
      "label": "Write Knowledge",
      "group": "knowledge",
      "description": "Create or update a Markdown note in the local knowledge base when the conversation produces reusable structured knowledge, stable project facts, procedures, or user-provided reference material. Prefer concise notes with source, summary, and cross-reference friendly paths.",
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "Optional Markdown path relative to the knowledge root, for example projects/nexo-agent/runtime-data.md. If omitted, a general path is generated from title."
          },
          "title": {
            "type": "string",
            "description": "Knowledge page title"
          },
          "content": {
            "type": "string",
            "description": "Markdown content to save"
          },
          "summary": {
            "type": "string",
            "description": "One-line summary used in knowledge/index.md"
          },
          "source": {
            "type": "string",
            "description": "Where the knowledge came from, such as conversation, document name, or URL"
          },
          "mode": {
            "type": "string",
            "enum": [
              "overwrite",
              "append"
            ],
            "description": "Write mode. Defaults to overwrite."
          },
          "updateIndex": {
            "type": "boolean",
            "description": "Whether to update knowledge/index.md and knowledge/log.md. Defaults to true."
          }
        },
        "required": [
          "content"
        ]
      }
    }
  ]
}
